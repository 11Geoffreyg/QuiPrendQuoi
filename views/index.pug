include ../partials/header.pug
h1 Qui prend quoi ?

h2 Créer un formulaire
form(method="post" action="/party")
    div
        label(for="name") Nom de l'événement
        input(name="name" id="name" required minlength="3" placeholder="Anniv Michou")
        label(for="author") Votre nom
        input(name="author" id="author" required minlength="3" placeholder="Admirateur secret")
        label(for="date") Date de l'événement
        input(type="date" name="date" id="date")
    button(type="submit") Créer